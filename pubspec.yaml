name: smart_swine
description: "A new Flutter project."
publish_to: 'none' # Evita publicar en pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # Declaración de assets
  assets:
    - lib/assets/logoSmartSwine.jpeg   # Logo de tu compañero
    - lib/assets/logo2SmartSwine.png   # Tu logo
